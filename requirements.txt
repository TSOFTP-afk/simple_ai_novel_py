PyQt6==6.11.0
python-docx==1.1.2
pytest>=8.0

# ============================================================
# RAG 向量检索系统
# 系统内置 FallbackEmbedder（无需额外安装可运行）
# 推荐安装以下依赖以启用 API Embedding 和 Reranker 功能：
# ------------------------------------------------------------
qdrant-client>=1.12.0   # Qdrant 客户端（本地嵌入式模式无需额外服务器）
jina>=3.0               # Jina AI 服务（Reranker 重排序）

# ============================================================
# Agent 系统依赖
# 无需额外安装，AgentGraph 为纯 Python 实现

# ============================================================
# 打包依赖
# pip install pyinstaller>=6.0    # PyInstaller 打包